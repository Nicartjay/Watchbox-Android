<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Only the update-APK directory is shared. Scoped deliberately narrowly:
      the extensions directory must never be exposed to other apps.
    -->
    <cache-path name="updates" path="updates/" />
</paths>
